

const load_heading = (greeting)=>
{
    return `${greeting}`

}

const myFunction = (greeting)=>
{
    return `${greeting}, testing `

}


function add_number(p1, p2) {
    return p1 + p2;
  }

export {load_heading, myFunction, add_number}